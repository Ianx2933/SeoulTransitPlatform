spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/Seoul_Transit
    username: postgres
    password: 330218
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect